# Application dependencies

require:
    - play
    - play -> crud
    - play -> secure
    - play -> db 1.1.1
    
repositories:
  - modules:
      type: local
      artifact: "c:/play_modules/Stundenplan/"
      contains:
          - modules -> *     
