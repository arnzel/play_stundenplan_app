# Application dependencies

require:
    - play
    - play -> crud
    - play -> secure
    
repositories:
  - modules:
      type: local
      artifact: "c:/play_modules/Stundenplan/"
      contains:
          - modules -> *     
