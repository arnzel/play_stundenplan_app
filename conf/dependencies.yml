# Application dependencies

require:
    - play
    - play -> crud
    
repositories:
  - modules:
      type: local
      artifact: "c:/play_modules/Stundenplan/"
      contains:
          - modules -> *     
